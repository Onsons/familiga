import { ArrierePlanDirective } from './arriere-plan.directive';

describe('ArrierePlanDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrierePlanDirective();
    expect(directive).toBeTruthy();
  });
});
